/*
* So here we will define a class to operate the declarations functionality. We will define functions to form up onto an API
* that we can use externally, so here we will also instantiate the puppeteer browser so that we can get the page instance
* from there.
*
* That said, lets code...
* */

export * from './whatsapp.client';
